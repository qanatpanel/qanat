## [1.1.1] - 2026-09-02

### 🐛 رفع باگ بحرانی: دکمهٔ «ورود به پنل» وسط صفحه نبود

- **ریشه:** اسکریپت بک‌گراند زنده (bgfx) کانواس و وینیت را بدون `position:fixed` به body اضافه می‌کرد. صفحهٔ فرود از `display:flex` برای وسط‌چین کردن کارت استفاده می‌کند و کانواس به‌عنوان آیتم flex، کل چیدمان را می‌شکست (کارت به لبهٔ راست هل داده می‌شد، عرض صفحه از viewport بیشتر می‌شد و دکمه له شده دیده می‌شد).
- **فیکس:** `position:fixed` به‌صورت inline روی کانواس/وینیت اعمال شد + قانون `.bg-canvas,.bg-vignette{position:fixed;inset:0;z-index:-1;pointer-events:none}` به CSS صفحهٔ فرود اضافه شد.
- **تأیید:** کارت در viewport های ۱۹۱۹px، ۱۲۸۰px، ۷۶۸px و ۳۹۰px در مرکز دقیق است (تست Playwright).
- تمام ۱۰ پنل فعال + بات با این نسخه دیپلوی شدند.

# 📜 تغییرات — Changelog

همه‌ی تغییرات مهم پروژه در این فایل ثبت می‌شود.

## [1.1.0] — 2026-09-02

### 🎨 ظاهر و تجربه کاربری
- بازطراحی کامل صفحه‌ی ورود: کارت دقیقاً وسط صفحه (افقی + عمودی)، برند عمودی با لوگو و تگ QANAT، آیکون قفل در فیلد رمز، خط نور متحرک بالای کارت، هاله‌ی بنفش، دکمه‌های شیشه‌ای در فوتر
- تم v3 — پس‌زمینه‌ی کانواس انیمیشنی اقیانوس (ذرات حساس به تم + صورت فلکی + درخشش موس) در همه‌ی صفحات
- لگوی گیت‌هاب در سایدبار پنل و فوتر لاگین؛ نشانگر «آخرین ورود» (localStorage)؛ لرزش خطا + اسپینر دکمه‌ی ورود

### 🔗 دسترسی سریع
- آلیاس `/Qanat` — باز کردن پنل با یک لمس از آدرس workers.dev بدون ۳۰۲ اضافه (مقاوم به شبکه‌های محدودکننده)
- دکمه‌ی «ورود به پنل» در صفحه‌ی اصلی؛ فیکس اسلش انتهایی در همه‌ی مسیرها
- تزریق `__SP__` به پنل تا API/لاگین/خروج نسبی از آدرس /Qanat هم کار کند

### 🛰 اسکنر IP تمیز
- بازطراحی کامل اسکنر هوشمند: اسکن ۳ فاز با یک کلیک (کالیبراسیون RTT مرورگر → تأیید TLS چندپورته روی کاندیدهای برتر → امتیاز ترکیبی A+ تا D)
- ست کردن IP روی کانفیگ‌ها: override سراسری (D1)، fallback خودکار در کانفیگ و اشتراک (اتصال به IP با حفظ SNI)، دکمه‌های 🎯 در اسکنر
- منابع چندگانه با کش D1 هر منبع + fallback کهنه؛ API جدید /panel/api/scan/status

### ⚙️ کانفیگ و پروکسی
- پورت‌های جایگزین کلودفلر (8443/2053/2083/2087/2096) برای failover
- پسورد Trojan معتبر ۵۶ هگز (مشتق از UUID) + fallback احراز هویت در رله
- Clash با url-test + قوانین ایران؛ sing-box با urltest + قوانین ایران
- خروجی کامل پکیج کانفیگ: GET /panel/api/export (شامل QR)
- هاپ‌های بالادستی (vless/trojan/transparent) با fetch+Upgrade

### 🔧 API و مدیریت
- PATCH /panel/api/users/:id — ویرایش کوتا، انقضا و یادداشت (مدیریت تمدید از تلگرام)

### 🛠 ابزارها و توسعه
- notify-update.mjs + فلگ --notify — اعلان نسخه‌ی جدید به همه‌ی کاربران بات بعد از دیپلوی
- deploy-panels.mjs: حفظ secret_text با keep_bindings + رد شدن (نه توقف) روی ورکرهای مشکل‌دار
- مستندات کامل دوزبانه (فارسی/انگلیسی)، LICENSE (MIT)، SECURITY.md، CONTRIBUTING.md و قالب‌های Issue/PR

## [1.0.0] — 2026-08-31

### افزوده‌ها
- هسته‌ی پروکسی WebSocket → TCP با VLESS و Trojan (پروتکل استاندارد رسمی)
- چندکاربره: کاربران، کوتای GB، انقضا، فعال/غیرفعال روی Cloudflare D1
- احراز هویت: PBKDF2 (100k)، JWT + HttpOnly Cookie، مسیر مخفی، کلایم توکن، بروت‌فورس، CSRF
- داشبورد فارسی RTL بدون وابستگی خارجی (نمای کلی / کاربران / اسکنر / اشتراک‌ها / تنظیمات)
- اسکنر IP تمیز: ۳ منبع (ircf.space / cf2dns / best-cf-ips) + پینگ واقعی مرورگر + کانفیگ با IP تمیز
- صفحه‌ی اشتراک با QR سمت سرور (SVG) و deep-link (Hiddify/v2box/v2rayNG/Happ)
- تولید کانفیگ: URI / Clash / sing-box / Xray
- ۹۵ تست خودکار (واحد + سناریو + E2E)
